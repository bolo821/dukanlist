import React from 'react';
import LeadProfile from '../components/LeadProfile';

const LeadScreens = () => {
    return (
        <div>
        hello
            <LeadProfile />
        </div>
    );
}

export default LeadScreens;
